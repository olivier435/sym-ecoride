{% extends 'base.html.twig' %}

{% block title %}Publier un trajet{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Publier un trajet</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Valider le trajet</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
